
timestep.maxtime   = 25.0   # how many steps to run?
timestep.viscous_cfl = 2.0
timestep.convection_cfl = 0.5

velocity.reynolds = 100.0

io.plot_file = plt
io.plot_dt_interval = 1.0   # how often to make plot files?

hypre.tol=1e-6
hypre.maxiter=20


n_cell        = 32  # number of cells in each dimension
max_grid_size = 32   # max grid size

# Must set coordinates to 0 (Cartesian), or 1 (Cylindrical), or 2 (Spherical).
geometry.coord_sys = 0

# Must set physical domain size.
geometry.prob_lo =  0.0   0.0   0.0
geometry.prob_hi =  1.0   1.0   1.0

# Periodic? Default in BoxLib is set to 0 (no).
geometry.is_periodic = 0  0  0
